-- Sample listings (requires auth users in production; uses placeholder UUIDs only for local dev)
-- Skip if no users exist — optional demo via admin posting from app

-- Promote admin helper (run manually after first login):
-- UPDATE profiles SET is_admin = true WHERE phone = '+91XXXXXXXXXX';
